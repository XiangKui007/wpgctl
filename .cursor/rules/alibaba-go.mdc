---
description: 阿里巴巴手册在本仓库 Go 侧的落地（命名、错误、控制语句）
globs: "**/*.go"
alwaysApply: false
---

# Go（对齐阿里巴巴 Java 手册精神）

`gofmt` / `goimports` 为准。包名短、全小写、不用下划线。导出 PascalCase，未导出 camelCase。

## 命名与常量

- 见名知意；禁止 `a`/`tmp`/`data1` 出现在包级或长生命周期变量。
- 魔法数字抽成具名常量；错误码、协议字面量集中定义。
- JSON/YAML 字段用 `json`/`yaml` tag，与现场配置键一致；不要为「纯 Java POJO」加无意义 getter。

## 控制语句（手册《控制语句》）

- `if` / `for` / `switch` 必须带大括号（Go 本就强制）。
- 优先早返回，避免深层嵌套（超过 3 层就拆函数）。
- `switch` 非穷尽时写 `default`（记录或返回明确错误）。
- 禁止用 `panic` 表达可预期失败；库代码只在不可恢复的编程错误时 `panic`。

## 错误与空值（手册《异常》《NPE》）

- 每个 `error` 必须处理：包装上下文 `fmt.Errorf("中文场景: %w", err)`，或显式忽略并写明原因。
- 禁止空 `catch`：禁止 `_ = do()` 后假装成功。
- 解引用指针、map、接口前判空；`err != nil` 时不要再使用不确定的返回值。
- 对外错误信息面向现场中文；日志可带主机、路径、模块名。

## 并发与资源

- 有 `go` 就必须有退出条件（`context`、`WaitGroup` 或生命周期注释）。
- `defer` 关闭文件/响应体；循环内 `defer` 谨慎（会拖到函数结束）。
- 共享状态用锁或 channel，不要文档里写「暂时不会竞态」。

## 测试

- `*_test.go` 表驱动；名称 `TestXxx_场景`。断言失败信息要能看出期望/实际。
