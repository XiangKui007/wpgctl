---
description: 阿里巴巴手册注释规约（Go godoc / JSDoc / Vue）
alwaysApply: true
---

# 注释（对齐阿里巴巴《注释规约》）

用中文把问题说清楚；专有名词（Docker、Nacos、Element Plus）保持英文。改代码必须同步改注释。

## 必须写

- **包 / 模块 / 文件**：说明职责与边界，不是复述目录名。
- **导出 API**（Go 导出符号、JS `export`、Vue `props`/对外方法）：说明做什么、关键参数、返回值、错误/异常。
- **枚举 / 状态机 / 魔法分支**：每个取值写用途。
- **非显而易见的为什么**：协议坑、现场约束、兼容旧数据。不要写「给 i 加 1」。

## 格式

- Go：包注释在 `package` 之上；导出符号用 `// Name ……。` 且**以符号名开头**（godoc）。方法内单行注释在语句**上方**另起一行，用 `//`。
- JS/Vue：文件头 `/** … */`；导出函数用 JSDoc（`@param` / `@returns`）；模板里不用大段 HTML 注释解释业务。
- 禁止用 `//` 充当导出 API 的完整说明时写半句英文或只写类型。

```go
// OpenFirewall 在对端打通 Nacos 8848/9848；失败返回可展示给现场的中文错误。
func OpenFirewall(host string) error { /* ... */ }
```

## 禁止

- 大段注释掉的死代码（直接删；必要时 git 可找回）。
- `@author` / `@date` 堆在每个函数上（包级说明即可）。
- 注释与实现不一致仍提交。
- `TODO` / `FIXME` 不写清意图：写成 `// TODO(谁): 做什么，阻塞原因`。
